development:
  driver: postgres
  open: user=rtravitz dbname=parkfinderdev sslmode=disable

test:
  driver: postgres
  open: user=rtravitz dbname=parkfindertest sslmode=disable

production:
  driver: postgres
  open: $PARKFINDER_PRODUCTION_DB
